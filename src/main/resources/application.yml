spring:
  # database config
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: posgrados

  # h2 console
  h2:
    console:
      enabled: true
      path: /h2-console

  # jpa sql format prettier
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
